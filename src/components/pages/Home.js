import React from 'react';

// Importing header section
import Header from '../sections/Header';

const Home = () => {
  return (
    <div className="home">
      <Header />
    </div>
  );
};

export default Home;
